import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Dark Mode / Obsidian palette
  static const Color background = Color(0xFF000000); // Obsidian Black
  static const Color surface = Color(0xFF101010); // Elevated black
  static const Color surfaceHighlight = Color(0xFF1A1A1A); // Lighter black

  // Kinetic Neon Lime palette
  static const Color primary = Color(0xFFCCFF00); // Neon Lime
  static const Color primaryDark = Color(0xFF99CC00); // Darker Lime
  static const Color accent = Color(0xFFCCFF00); 

  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFA0A0A5); // Light Gray
  static const Color textDisabled = Color(0xFF4C4C50); // Dark Gray

  static const Color error = Color(0xFFFF453A);
  static const Color success = Color(0xFF32D74B); // SF Pro Success Green
}
