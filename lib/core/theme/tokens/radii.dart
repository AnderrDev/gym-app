/// Escala de radios de borde. La pill `circular(999)` es para chips/tags
/// y CTAs muy redondeados.
class Radii {
  Radii._();

  /// 4 — pequeños highlights.
  static const double xs = 4;

  /// 8
  static const double sm = 8;

  /// 12
  static const double md = 12;

  /// 16 — cards estándar.
  static const double lg = 16;

  /// 20 — hero cards, botones grandes.
  static const double xl = 20;

  /// 32 — bottom sheets.
  static const double xxl = 32;

  /// Pill (≈ infinito).
  static const double pill = 999;
}
